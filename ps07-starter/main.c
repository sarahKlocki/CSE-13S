#include "string2.h"
#include <stdio.h>

int main(void) {
    /* ───────── strlen checks ───────── */

    /* ───────── strstr checks ───────── */

    /* ───────── strncpy checks ───────── */

    /* ───────── strcmp checks ───────── */

    return 0;
}